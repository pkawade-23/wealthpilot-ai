services:
  mongodb:
    image: mongo:8.0

    container_name: wealthpilot-mongodb

    restart: unless-stopped

    ports:
      - "27017:27017"

    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: